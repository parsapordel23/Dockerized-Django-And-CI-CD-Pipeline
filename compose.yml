services:
  web:
    image: ${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_NAME}
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/app
    ports:
      - "8000:8000"